# Core Deep Learning Framework
# torch==2.12.0+cu132          CUDA 13.2 / RTX 5060 Blackwell，need to be downloaded separately, as pip does not contain torch+cu132

# Data Processing
numpy==2.1.0
pandas==2.2.3                # Core library for CSV & time series
pyarrow==17.0.0              # Accelerates pandas CSV loading

# Models (Gradient Boosting + Deep Learning)
xgboost==2.1.3
lightgbm==4.5.0

fredapi~=0.5.2